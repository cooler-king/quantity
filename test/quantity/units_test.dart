import 'package:test/test.dart';

void main() {
  group('units', () {
    /*
    test('constructors', () {
      const Units u = const Units("test", "abbrev", "symbol", "singular",
          10.0, Scalar.SCALAR_DIMENSIONS, false);
      expect(u, isNotNull);

    });
    */
  });
}
